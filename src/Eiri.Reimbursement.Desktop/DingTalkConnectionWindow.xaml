<Window x:Class="Eiri.Reimbursement.Desktop.DingTalkConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="连接钉钉接口" Width="520" Height="440" MinWidth="420" MinHeight="400"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SurfaceBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Client ID" Target="{Binding ElementName=ClientIdInput}" Padding="0,0,0,6"/>
        <TextBox x:Name="ClientIdInput" Grid.Row="1" Height="36" AutomationProperties.Name="Client ID"/>
        <Label Grid.Row="2" Content="Client Secret" Target="{Binding ElementName=ClientSecretInput}" Padding="0,14,0,6"/>
        <PasswordBox x:Name="ClientSecretInput" Grid.Row="3" Height="36" Padding="8"
                     Background="{DynamicResource InputBrush}" Foreground="{DynamicResource PrimaryTextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}" AutomationProperties.Name="Client Secret"/>
        <Label Grid.Row="4" Content="AccessToken" Target="{Binding ElementName=AccessTokenOutput}" Padding="0,14,0,6"/>
        <TextBox x:Name="AccessTokenOutput" Grid.Row="5" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" AutomationProperties.Name="AccessToken"/>
        <TextBlock x:Name="StatusText" Grid.Row="6" Margin="0,12,0,12" TextWrapping="Wrap"
                   AutomationProperties.LiveSetting="Polite"/>
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ConnectButton" Content="确定" MinWidth="88" IsDefault="True" Click="Connect_OnClick"/>
            <Button Content="关闭" MinWidth="88" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
